# Frontend README

## Installation

To run the TypeScript script, you need to have Node.js and npm installed. Then, install TypeScript and ts-node globally:

```bash
npm install -g typescript ts-node
```

## Running the Script

To run the display script, use the following command:

```bash
ts-node frontend/display.ts
```
